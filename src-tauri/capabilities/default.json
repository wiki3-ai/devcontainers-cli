{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Default capability for the Devcontainers app main window. Grants the WebView access to the in-tree Tauri commands and the dialog plugin used by the 'Open Folder…' action.",
	"windows": ["main"],
	"permissions": [
		"core:default",
		"core:event:default",
		"core:window:default",
		"core:webview:default",
		"core:app:default",
		"dialog:default"
	]
}
