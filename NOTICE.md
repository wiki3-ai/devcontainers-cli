# NOTICE

Devcontainers.app is a fork of `devcontainers/cli` by Microsoft (MIT-licensed,
see `LICENSE.txt` and `ThirdPartyNotices.txt`) being converted into a Tauri 2
macOS desktop application. The surviving TypeScript code in `spec/` retains
its original Microsoft copyright headers and MIT license. New Rust code in
`src-tauri/` and the Tauri/Vite scaffolding under `frontend/` are likewise
MIT-licensed.

The scope of the fork is documented in `ROADMAP.md`.
