[package]
name = "parse-config-rust"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
devcontainer-config = { path = "../../perry-bridge/rust/devcontainer-config", default-features = false, features = ["embed"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
